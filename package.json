{
  "name": "@motionwerk/sharecharge-core-client",
  "version": "0.9.0",
  "description": "Share & Charge Core Client for Charge Point Operators",
  "main": "index.js",
  "scripts": {
    "postinstall": "([ -e dist/app.js ] && chmod +x ./dist/app.js) || echo 'No tsc output found. Ignoring.'",
    "start": "./dist/app.js",
    "test": "mocha --exit -r ts-node/register test/**/*.spec.ts"
  },
  "bin": {
    "sc-cc": "./dist/app.js"
  },
  "author": "MotionWerk GmbH",
  "license": "MIT",
  "dependencies": {
    "@motionwerk/sharecharge-api": "^0.3.0",
    "@motionwerk/sharecharge-common": "^1.13.4",
    "@motionwerk/sharecharge-example-bridge": "^0.4.2",
    "@motionwerk/sharecharge-lib": "^0.8.0"
  },
  "devDependencies": {
    "@types/chai": "^4.1.4",
    "@types/mocha": "^5.2.2",
    "chai": "^4.1.2",
    "mocha": "^5.1.1",
    "ts-node": "^6.1.2",
    "typescript": "^2.9.2"
  }
}
