import { {{bridge.name}} } from '{{bridge.path}}';

export let config = {
    test: {{test}},
    bridge: {{bridge.name}},
    statusUpdateInterval: {{statusUpdateInterval}},
    connectors: '{{connectorData}}'
}