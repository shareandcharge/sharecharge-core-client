import Bridge from '{{ bridge }}';

export let config = {
    test: {{ test }},
    bridge: Bridge,
    'status-interval': {{ statusInterval }},
    connectors: '{{ connectorData }}',
    id: '{{ id }}',
    pass: '{{ pass }}'
}